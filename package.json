{
  "name": "app",
  "private": true,
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "esbuild": "^0.24.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* app/javascript/modules/*.* --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets"
  },
  "dependencies": {
    "@hotwired/turbo-rails": "^8.0.12"
  }
}